import type { FurniturePageProps } from '@/components/react/interfaces';

export default function FurniturePage({ baseApiUrl }: FurniturePageProps) {

    return (
        <main>
            Form to create Products
        </main>
    )
}

